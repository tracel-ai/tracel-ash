name: publish

on:
  push:
    tags:
      - "v*"

jobs:
  publish-tracel-ash:
    uses: tracel-ai/github-actions/.github/workflows/publish-crate.yml@v11
    with:
      crate: tracel-ash
      tag-validation: false
    secrets: inherit
